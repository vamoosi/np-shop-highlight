{
  "name": "np-shop-highlight",
  "version": "1.0.2",
  "devDependencies": {
    "@types/chrome": "0.0.95",
    "typescript": "3.7.5",
    "webpack": "^4.41.6",
    "webpack-cli": "^3.3.11"
  },
  "scripts": {
    "compile": "rm -rf build; ./node_modules/.bin/tsc",
    "pack": "rm -rf dist; ./node_modules/.bin/webpack --config webpack.config.js --mode production",
    "prod": "npm run compile && npm run pack && cp -r -t dist manifest.json res",
    "dist": "rm -rf bin; npm run prod && mkdir bin && cd dist && zip -r ../bin/np-shop-highlight-`jq -r .version manifest.json`.zip *"
  }
}
